# A chrome extension to help getting current tab URL 

## What I learned:
    > const
    > addEventListener()
    > innerHTML
    > input.value
    > functiion parameter, passing argument 
    > template string `${}`
    > localStorage
    > The JSON Object
    > object in arrays (ex: tabs[0].url)